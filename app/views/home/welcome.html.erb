<div class="capa">
  <img src="/assets/capa.jpg" class="img-fluid vw-100" alt="Capa - Paisagem">
</div>
<div class="border">
  <h4>Últimas postagens</h4>
  <div class="container text-center">
    <div class="row align-items-center">
      <% @posts.each do |post| %>
        <div class="col">
          <% if post.image.attached? %>
            <%= image_tag post.image, class:"card-img"%>
          <% end %>
          <%= post.title %>
        </div>
      <% end %>
    </div>
  </div>
</div>
